# 路径配置
[Path]
conf_dir = ././conf/
file_dir = ././file/
log_dir = ././log/
report_dir = ././report/
testcase_dir = ././testcase/

# Mysql配置
[mysql]
host = 10.17.4.150
port = 3306
user = root
password = *9C5C2dC
database = coolvideo

# 跳板机
[ssh]
ssh_host = "rickychen.relay.kugou.net"
ssh_port = "32200"
ssh_user = "rickychen"
ssh_pwd = "p6cIZvpxS)l0"



# 包名获取：adb shell pm list packages -3
# 查询app进程号：adb shell ps | grep "com.kugou.android.ktvapp"
# 创建文件：touch /sdcard/Pictures/app.log
# 授予权限：chmod 777 /sdcard/Pictures/app.log
# 导入日志：adb logcat | grep -e "3219" -e "1419" >> /sdcard/Pictures/app.log
#  adb shell monkey -p "com.kugou.android.ktvapp" -v -v -v 1000 > /sdcard/Pictures/app.log




